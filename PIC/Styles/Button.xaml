<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    
    <!-- BOTONS DEL MENÚ PRINCIPAL -->
    <Style x:Key="Main_Menu_Button" TargetType="RadioButton">

        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,0,0,0"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">

                    <Grid x:Name="GridArea" Height="40" Width="230">

                        <Grid.Background>
                            <SolidColorBrush x:Name="GridBackground"
                                         Color="{DynamicResource ColorMainBotoBase}"/>
                        </Grid.Background>

                        <Grid>
                            <ContentPresenter/>
                        </Grid>

                    </Grid>

                    <ControlTemplate.Triggers>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="GridArea" Property="Background" Value="{DynamicResource MainBotoHoover}"/>
                        </Trigger>

                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Background" TargetName="GridArea" Value="{DynamicResource MainBotoChecked}"/>
                        </Trigger>

                    </ControlTemplate.Triggers>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- BOTONS ACCIONS -->
    <Style x:Key="Accions_Button" TargetType="Button">

        <Setter Property="Foreground" Value="{DynamicResource MigClar}"/>
        <Setter Property="Background" Value="{DynamicResource BotoBase}"/>
        <Setter Property="BorderThickness" Value="0"/>


        <Setter Property="FontFamily" Value="/PIC;component/Fonts/#PP Fraktion Mono"/>
        <Setter Property="FontSize" Value="12"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">

                    <Grid x:Name="GridArea"
                            Background="{TemplateBinding Background}">

                        <ContentPresenter HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Margin="{TemplateBinding Padding}"/>

                    </Grid>

                    <ControlTemplate.Triggers>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="GridArea" Property="Background" Value="{DynamicResource BotoHoover}"/>
                        </Trigger>

                    </ControlTemplate.Triggers>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- BOTO ARXIU -->
    <Style x:Key="Arxiu_Button" TargetType="Button">

        <Setter Property="Foreground" Value="#131316"/>
        <Setter Property="Background" Value="#e8e9e9"/>
        <Setter Property="BorderThickness" Value="0"/>


        <Setter Property="FontFamily" Value="/PIC;component/Fonts/#PP Fraktion Mono"/>
        <Setter Property="FontSize" Value="12"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">

                    <Grid x:Name="GridArea"
                            Background="{TemplateBinding Background}">

                        <ContentPresenter HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Margin="{TemplateBinding Padding}"/>

                    </Grid>

                    <ControlTemplate.Triggers>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="GridArea" Property="Background" Value="#a5a6af"/>
                        </Trigger>

                    </ControlTemplate.Triggers>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- BOTÓ ALERTA -->
    <Style x:Key="Alerta_Button" TargetType="Button">

        <Setter Property="Foreground" Value="{DynamicResource Clar}"/>
        <Setter Property="Background" Value="{DynamicResource Alerta}"/>
        <Setter Property="BorderThickness" Value="0"/>


        <Setter Property="FontFamily" Value="/PIC;component/Fonts/#PP Fraktion Mono"/>
        <Setter Property="FontSize" Value="12"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">

                    <Grid x:Name="GridArea"
                            Background="{TemplateBinding Background}">

                        <ContentPresenter HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Margin="{TemplateBinding Padding}"/>

                    </Grid>

                    <ControlTemplate.Triggers>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="GridArea" Property="Background" Value="{DynamicResource AlertaClar}"/>
                        </Trigger>

                    </ControlTemplate.Triggers>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- BOTÓ REPARAR -->
    <Style x:Key="Reparar_Button" TargetType="Button">

        <Setter Property="Foreground" Value="{DynamicResource Clar}"/>
        <Setter Property="Background" Value="{DynamicResource Reparar}"/>
        <Setter Property="BorderThickness" Value="0"/>


        <Setter Property="FontFamily" Value="/PIC;component/Fonts/#PP Fraktion Mono"/>
        <Setter Property="FontSize" Value="12"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">

                    <Grid x:Name="GridArea"
                            Background="{TemplateBinding Background}">

                        <ContentPresenter HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Margin="{TemplateBinding Padding}"/>

                    </Grid>

                    <ControlTemplate.Triggers>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="GridArea" Property="Background" Value="{DynamicResource RepararClar}"/>
                        </Trigger>

                    </ControlTemplate.Triggers>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- BOTÓ CONFIRMAR -->
    <Style x:Key="Confirmar_Button" TargetType="Button">

        <Setter Property="Foreground" Value="#e8e9e9"/>
        <Setter Property="Background" Value="#1fb9f9"/>
        <Setter Property="BorderThickness" Value="0"/>


        <Setter Property="FontFamily" Value="/PIC;component/Fonts/#PP Fraktion Mono"/>
        <Setter Property="FontSize" Value="12"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">

                    <Grid x:Name="GridArea"
                        Background="{TemplateBinding Background}">

                        <ContentPresenter HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Margin="{TemplateBinding Padding}"/>

                    </Grid>

                    <ControlTemplate.Triggers>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="GridArea" Property="Background" Value="#5bcdfc"/>
                        </Trigger>

                    </ControlTemplate.Triggers>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- BOTÓ NOTIFICACIÓ -->
    <Style x:Key="Notificacio_Button" TargetType="Button">

        <Setter Property="Foreground" Value="{DynamicResource MigClar}"/>
        <Setter Property="Background" Value="{DynamicResource Notificacio}"/>
        <Setter Property="BorderThickness" Value="0"/>


        <Setter Property="FontFamily" Value="/PIC;component/Fonts/#PP Fraktion Mono"/>
        <Setter Property="FontSize" Value="12"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">

                    <Grid x:Name="GridArea"
                        Background="{TemplateBinding Background}">

                        <ContentPresenter HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Margin="{TemplateBinding Padding}"/>

                    </Grid>

                    <ControlTemplate.Triggers>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="GridArea" Property="Background" Value="{DynamicResource NotificacioClar}"/>
                        </Trigger>

                    </ControlTemplate.Triggers>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>